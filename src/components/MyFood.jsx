function MyFood({ name, origin, nickname }) {
  return (
    <div>
      <p>
        The name of my food is {name}. It originated from {origin}. It is
        popularly known as {nickname}.
      </p>
    </div>
  );
}

export default MyFood;
